<view class="tab-control">
  <block wx:for="{{titles}}" wx:key="{{index}}">
    <view bind:tap="handleTap" 
         data-index="{{index}}" 
        class="control-item {{currentIndex == index?'active':''}}">
      <text>{{item}}</text>
    </view>
  </block>
</view>